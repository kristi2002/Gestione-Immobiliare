# Normalize all text files to LF in the repo (prevents OneDrive/Windows
# silently rewriting the whole tree to CRLF). Binary files are auto-detected.
* text=auto eol=lf
*.sh text eol=lf
docker/docker-entrypoint.sh text eol=lf
