# Verification report — cold-start + demo dry run — 2026-07-12

Run against a **fresh** `php:8.3-apache` + **MySQL 8** stack via `docker compose` (DB initialised from
`database/schema_production.sql`, then `php database/migrate.php`). Admin created via `setup.php`.
Everything below was **executed**; results are HTTP codes / response bodies, not assumptions (CLAUDE.md §0).

## Stop-the-line findings
**None.** No IDOR, no public-document exposure, no money duplication surfaced.

## Tier 1 — auth / isolation / money / documents
| Test | Command | Expected | Actual | PASS/FAIL |
|---|---|---|---|---|
| Admin login | `POST /login.php` | 302 → index, session set | 302, cookie `gestionale_session` | PASS |
| Boundary: stats no cookie | `GET /api/get_dashboard_stats.php` | 401 | 401 | PASS |
| Boundary: stats w/ cookie | same + cookie | 200 | 200 | PASS |
| Boundary: raw view | `GET /views/dashboard.html` | 403 | 403 | PASS |
| Boundary: config | `GET /config/db.php` | 403 | 403 | PASS |
| Boundary: setup locked | `GET /setup.php` | 403 | 403 | PASS |
| CSRF: POST w/o token | `POST /api/clients.php` | reject | 403 "Token CSRF non valido" | PASS |
| Doc download no cookie | `GET /api/download_document.php?id=1` | 401 | 401 | PASS |
| **Persisted invoice XML direct** | `GET /uploads/documents/fatture/IT…_00006.xml` | 403 | **403** | PASS |
| Media guard bogus id | `GET /api/media.php?id=999999` | 4xx (no 500) | 404 | PASS |

> The persisted FatturaPA XML being **403 by direct URL** is the live confirmation of the §4.5 uploads
> boundary that was previously "could not verify" — now verified on the real Apache deployment.

## Tier 2 — cold-start + demo path (fresh DB, zero seed)
| Step | Result |
|---|---|
| Apply migrations 29→39 on fresh schema | **38 applied, 0 pending**; phase35–39 recorded; new tables/columns all present |
| Create proprietario | `success` (client #19) |
| Create immobile (with dati catastali) | `success` (property #8); `cadastral_foglio=12`, `cadastral_zone=B1` persisted |
| Generate property PDF | `success`, `download` link returned |
| OMI quotazione → valuation estimate | value **€175 750**, rent **€808**, basis `OMI` (1600–2100 €/m² × 95 m²) — math correct |
| AML record create | `success`, `retention_until=2036-07-12` (verifica +10 anni, auto) |
| **FatturaPA lifecycle** | invoice #6 `FAT-2026-0001` → generate `generato` (`IT01234567890_00006.xml`) → transmit(manual) stays `generato` channel `manuale` (no fake "sent") → record `RC` → **`consegnato`** + `delivered_at` |
| Authenticated FE XML download | 200 `application/xml`, valid `<p:FatturaElettronica>` |
| AI describe, no key | graceful **400** "AI non configurata" (no 500, no dead UI) |

New endpoints smoke test (GET): `scadenzario`, `valuation`, `valuation?estimate`, `aml`, `portal_sync`,
`properties?matching_leads`, `fattura_sdi?list`, `generate_sdd?check` — **all 200**.

App error log during the whole run: **no PHP Warning/Fatal/Notice/Uncaught** entries.

## Tier 3 — integrations
- **Email:** `MAIL_ENABLED=false` in this stack → correctly not sent; present as "in attivazione".
- **AI / FatturaPA-SdI / SDD:** work in "manual/not-configured" mode with honest messages; auto modes need keys.

## Could NOT verify
- Live **SMTP inbox delivery**, real **SdI intermediary** transmission, real **portal push**, and a
  **readonly-role** write-rejection at the API (no readonly user was created this run) — all need
  external credentials or extra setup. None block the demo.

## Discrepancies with the docs — resolved
- Admin session cookie is **`gestionale_session`** (confirmed live) — matches `ARCHITECTURE.md`, not the
  `gi_session` in `DEPLOY.md`. Use `gestionale_session`.
- "uploads/ served without auth" (old GAPS/PRODUCTION_READINESS) is **false** for the documents tree —
  it returns 403 (verified). Only `uploads/properties/` images are public by design.
