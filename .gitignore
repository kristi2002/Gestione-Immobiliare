.env
.env.docker
.env.production
/vendor/
backups/
uploads/properties/*
uploads/documents/*
uploads/social/*
!uploads/properties/.gitkeep
!uploads/documents/.gitkeep
!uploads/branding/.gitkeep
!uploads/documents/generated/.gitkeep
.setup_complete
.claude/
.phpunit.result.cache
*.log

# Design reference mockups — kept locally, never deployed or tracked
immobiliare design/

# Local dev scratch (port-conflict workaround, not for CI/prod)
docker-compose.dryrun.yml
